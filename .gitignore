# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (never commit)
.env
.env.local
.env.*.local

# Playwright auth state (session tokens — never commit)
.auth/

# Playwright test results
test-results/
playwright-report/

# Playwright compiled TypeScript
e2e/**/*.js
e2e/**/*.d.ts
e2e/tsconfig.tsbuildinfo

# Test coverage reports
coverage/

# SDD artifacts (local planning, not for version control)
sdd/

# Deno lockfile
deno.lock

# Atl (skill registry, non-shareable artifacts)
.atl/

# Pi / OpenCode local runtime artifacts
.engram/
.pi-lens/
e2e/.pi-lens/
.codegraph/
.opencode/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

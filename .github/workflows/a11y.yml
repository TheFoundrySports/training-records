name: Accessibility

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  axe-core-scan:
    name: Axe-core CLI Scan (WCAG 2.2 AA)
    runs-on: ubuntu-latest

    env:
      VITE_SUPABASE_URL: http://127.0.0.1:54321
      VITE_SUPABASE_ANON_KEY: sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Start dev server
        run: pnpm run dev &
        env:
          CI: 'true'

      - name: Wait for dev server
        run: pnpm dlx wait-on http://localhost:5173 --timeout 30000

      - name: Run axe-core CLI on Login
        run: pnpm exec axe http://localhost:5173/login --exit

      - name: Run axe-core CLI on Workout List
        run: pnpm exec axe http://localhost:5173/workouts --exit

      - name: Run axe-core CLI on Workout Detail
        run: pnpm exec axe http://localhost:5173/workouts/00000000-0000-0000-0000-000000000001 --exit

      - name: Run axe-core CLI on Workout Form
        run: pnpm exec axe http://localhost:5173/workouts/new --exit

      - name: Run axe-core CLI on Calendar
        run: pnpm exec axe http://localhost:5173/calendar --exit

      - name: Stop dev server
        if: always()
        run: pkill -f "vite" || true
